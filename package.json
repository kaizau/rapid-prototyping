{
  "name": "",
  "version": "0.0.1",
  "description": "",
  "author": "",
  "repository": "",
  "private": true,
  "scripts": {
    "start": "now dev --listen 8888",
    "debug": "now dev --listen 8888 --debug",
    "lint": "prettier --write '*.{json,md,yml,yaml,graphql}' & eslint --fix .",
    "build": "npm run clean && parcel build 'site/**.html' --out-dir public --no-source-maps --no-cache",
    "watch": "npm run clean && parcel watch 'site/**.html' --out-dir public --no-source-maps --no-cache",
    "clean": "rimraf public"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.17.1",
    "helmet": "^4.0.0"
  },
  "devDependencies": {
    "eslint": "^7.6.0",
    "eslint-config-prettier": "^6.11.0",
    "parcel-bundler": "^1.12.4",
    "prettier": "^2.0.5",
    "rimraf": "^3.0.2"
  }
}
