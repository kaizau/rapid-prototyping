{
  "name": "",
  "version": "1.0.0",
  "author": "k@kaizau.com",
  "repository": "",
  "description": "",
  "private": true,
  "scripts": {
    "start": "npm run clean && npm run lint && npm run dev-site",
    "build": "npm run clean && npm run dev-build",
    "dev-site": "node -r now-env node_modules/.bin/parcel watch site/{,**/}*.pug --no-source-maps",
    "dev-api": "node -r now-env node_modules/.bin/now-lambda --port 8889",
    "dev-build": "node -r now-env node_modules/.bin/parcel build site/{,**/}*.pug --no-source-maps",
    "clean": "rimraf dist/*",
    "lint": "eslint --fix .",
    "now-build": "npm run clean && parcel build site/**.pug --no-source-maps"
  },
  "devDependencies": {
    "eslint": "^5.14.1",
    "now-env": "kaizau/now-env",
    "now-lambda-runner": "^3.3.0",
    "parcel": "^1.12.0",
    "pug": "^2.0.3",
    "rimraf": "^2.6.3",
    "stylus": "^0.54.5"
  },
  "dependencies": {
    "mithril": "^1.1.6",
    "normalize.css": "^8.0.1"
  }
}
