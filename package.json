{
  "name": "kz-vercel-template",
  "version": "0.0.1",
  "description": "",
  "author": {
    "name": "Kai Zau",
    "email": "k@kaizau.com",
    "url": "https://kaizau.com"
  },
  "repository": "",
  "private": true,
  "scripts": {
    "start": "vercel dev --listen 8888",
    "dev": "npm run clean && parcel --port $PORT 'site/**.html' --out-dir public --no-cache",
    "build": "npm run clean && parcel build 'site/**.html' --out-dir public --no-source-maps --no-cache",
    "lint": "prettier --ignore-path .gitignore --write . && eslint --fix .",
    "test": "jest --watch",
    "clean": "rimraf public"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.17.1",
    "helmet": "^4.0.0"
  },
  "devDependencies": {
    "eslint": "^7.6.0",
    "eslint-config-prettier": "^6.11.0",
    "jest": "^26.2.2",
    "parcel-bundler": "^1.12.4",
    "prettier": "^2.0.5",
    "rimraf": "^3.0.2",
    "stylus": "^0.54.8"
  }
}
